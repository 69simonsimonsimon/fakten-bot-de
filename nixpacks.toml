[phases.setup]
nixPkgs = ["python312", "ffmpeg", "fontconfig", "freetype", "libGL", "libGLU", "glib"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -m playwright install chromium --with-deps"
]

[start]
cmd = "python dashboard/app.py"
