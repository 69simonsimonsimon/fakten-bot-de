[phases.setup]
nixPkgs = ["python312", "ffmpeg", "chromium", "noto-fonts", "noto-fonts-cjk-sans"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -m playwright install chromium"
]

[start]
cmd = "python dashboard/app.py"
